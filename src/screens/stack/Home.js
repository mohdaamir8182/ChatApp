import React from 'react'
import { View, Text } from 'react-native'
import { BottomTabsNavigator } from '../../../Routes';

const Home = () => {
    return (
        <BottomTabsNavigator />
    )
}

export default Home;
